#include <iostream>
#include "linkedList.hpp"
#include "importLL.hpp"

int main()
{
    CreateLinkedList();
    SortLinkedList(llstart);
    DisplayAll(llstart);
    system("pause");
    return 0;
}

